# ── Core ────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic==2.11.3
numpy>=1.26.0
pandas>=2.1.0
joblib>=1.3.0

# ── Data bronnen ────────────────────────────────────────
yfinance>=0.2.40
ccxt>=4.1.0
feedparser>=6.0.0
pandas-datareader>=0.10.0

# ── Database ────────────────────────────────────────────
psycopg2-binary>=2.9.0

# ── ML / Signaalmodellen ────────────────────────────────
lightgbm>=4.1.0
scikit-learn>=1.3.0
shap>=0.43.0
imbalanced-learn>=0.12.0

# ── RL / Portfolio management ───────────────────────────
# torch/stable-baselines3/sb3-contrib/gymnasium zijn te zwaar voor Render
# (torch alleen al ~300-500 MB RAM). De code degradeert graceful naar
# LightGBM + regelgebaseerde fallbacks als deze pakketten ontbreken.
# Installeer lokaal of in een GPU-omgeving voor training.
# gymnasium>=0.29.0
# stable-baselines3>=2.2.0
# sb3-contrib>=2.2.0

# ── NLP / Sentiment ────────────────────────────────────
# FinBERT via transformers + torch vergt ~800 MB RAM, niet geschikt voor Render.
# transformers>=4.36.0
# torch>=2.1.0
